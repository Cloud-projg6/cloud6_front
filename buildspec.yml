version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  build:
    commands:
      - cd cloud6_front
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: cloud6_front/dist